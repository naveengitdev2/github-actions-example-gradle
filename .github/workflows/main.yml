# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: project cicd flow

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
   build:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@master

         - uses: Azure/docker-login@v1
           with:
              login-server: acrloki.azurecr.io
              username: acrloki
              password: Siq/3W4+ntE9YfGNrbSpQFMHkOfm/YrdC9CheVOCmw+ACRALPmul

         - run: |
              docker build . -t acrloki.azurecr.io/k8sdemo:${{ github.sha }}
              docker push acrloki.azurecr.io/k8sdemo:${{ github.sha }}

#          - uses: azure/setup-kubectl@v2.0

#          - uses: Azure/k8s-set-context@v2
#            with:
#               kubeconfig: ${{ secrets.KUBE_CONFIG }}

#          - uses: Azure/k8s-create-secret@v1.1
#            with:
#               container-registry-url: acrloki.azurecr.io
#               container-registry-username: acrloki
#               container-registry-password: Siq/3W4+ntE9YfGNrbSpQFMHkOfm/YrdC9CheVOCmw+ACRALPmul
#               secret-name: demo-k8s-secret

#          - uses: Azure/k8s-deploy@v4
#            with:
#               action: deploy
#               manifests: |
#                  manifests/deployment.yml
#                  manifests/service.yml
#               images: |
#                  demo.azurecr.io/k8sdemo:${{ github.sha }}
#               imagepullsecrets: |
#                  demo-k8s-secret
