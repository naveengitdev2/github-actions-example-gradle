# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: project cicd flow

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: '1.8'
        distribution: 'adopt'
        cache: maven
    - name: Build with Maven
      run: mvn clean install

#     - uses: azure/docker-login@v1
#       with:
#         login-server: 'acrloki.azurecr.io' # default: index.docker.io
#         username: 'acrloki'
#         password: 'Siq/3W4+ntE9YfGNrbSpQFMHkOfm/YrdC9CheVOCmw+ACRALPmul'
    
#     - uses: azure/docker-login@v1
#       with:
#         login-server: acrloki.azurecr.io
#         username: acrloki
#         password: Siq/3W4+ntE9YfGNrbSpQFMHkOfm/YrdC9CheVOCmw+ACRALPmul

#     - run: |
#         docker build . -t acrloki.azurecr.io/k8sdemo:${{ github.sha }}
#         docker push acrloki.azurecr.io/k8sdemo:${{ github.sha }}

    - name: Azure Container Registry Build
      uses: Azure/acr-build@v1
      with:
        # Service Principal with Contributor role on the ACR
        service_principal: 113e4b2d-a366-485c-8aa7-2481e0629d2c
        # Service Principal password
        service_principal_password: cCE8Q~b5EkvgbLVLe4OdBRTnsmwuhNfc0NFe_bsR
        # Azure Container Registry tenant
        tenant: f4eebf37-195a-4f44-b689-0540703f8207
        # The name of the ACR, minus the .azurecr.io
        registry: acrloki
        # Repository to use
        repository: https://github.com/naveengitdev2/github-actions-example.git
#         # Github access token for private repositories
#         git_access_token: 
        # The folder in the Github repo that holds the source
        folder: .github/workflows/maven.yml
